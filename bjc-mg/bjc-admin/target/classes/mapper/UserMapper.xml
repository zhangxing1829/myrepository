<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bjc.mapper.UserMapper">

	<!-- 通用查询映射结果 -->
	<resultMap id="BaseResultMap" type="com.bjc.entity.User">
		<id column="id" property="uId" />
		<result column="username" property="username" />
		<result column="nickname" property="nickname" />
		<result column="password" property="password" />
		<result column="salt" property="salt" />
		<result column="email" property="email" />
		<result column="status" property="status" />
		<result column="gmt_create" property="gmtCreate" />
		<result column="gmt_modified" property="gmtModified" />
	</resultMap>

	<select id="selectUsers" resultType="com.bjc.entity.User">
		select tbl_sys_user.u_id uId,
			tbl_sys_user.username,
			tbl_sys_user.password,
			tbl_sys_user.email,
			tbl_sys_user.status,
			tbl_sys_user.number
		from tbl_sys_user
	</select>
	<insert id="addUser" parameterType="com.bjc.entity.User">
		insert into tbl_sys_user(username, email, number,password, status)
			value (#{username},#{email},#{number},#{password},0)
	</insert>

</mapper>
